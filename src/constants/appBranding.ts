/** User-facing app name (App Store, splash, home). */
export const APP_DISPLAY_NAME = "Scroll The Wheel";

/** Expo slug / deep-link scheme (no spaces). */
export const APP_SLUG = "scroll-the-wheel";
export const APP_SCHEME = "scrollthewheel";

/** iOS / Android application id for store builds. */
export const APP_BUNDLE_ID = "com.scrollthewheel.app";

export const APP_LOGO = require("../../assets/images/logow.png");
