import storeListing from "../../store/metadata/en-US.json";

/** Store listing copy — source of truth: `store/metadata/en-US.json` */
export const STORE_LISTING = storeListing;

export const STORE_TAGLINE = storeListing.subtitle;
export const STORE_SHORT_DESCRIPTION = storeListing.shortDescription;
